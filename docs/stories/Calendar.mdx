import { Meta, Canvas, Story } from '@storybook/blocks';
import * as CalendarStories from './Calendar.stories';

<Meta of={CalendarStories} />

# Calendar

날짜 선택 캘린더 컴포넌트입니다. 단일 날짜 선택과 날짜 범위 선택 모드를 지원합니다.

## Usage

```tsx
import { Calendar } from '@ubittz/design-system';

function Example() {
  const [date, setDate] = useState<Date | null>(null);

  return <Calendar value={date} onChange={setDate} />;
}
```

## Single Select

기본 단일 날짜 선택 모드입니다.

<Canvas of={CalendarStories.SingleSelect} />

## Range Select

`mode="range"`로 날짜 범위 선택이 가능합니다.

```tsx
const [range, setRange] = useState<[Date, Date] | null>(null);

<Calendar mode="range" value={range} onChange={setRange} />;
```

<Canvas of={CalendarStories.RangeSelect} />

## With Buttons

`showButton`으로 하단 취소/확인 버튼을 표시합니다.

<Canvas of={CalendarStories.WithButtons} />

## Without Day Header

`showDayHeader={false}`로 요일 헤더를 숨길 수 있습니다.

<Canvas of={CalendarStories.WithoutDayHeader} />

## Min/Max Date

`minDate`와 `maxDate`로 선택 가능한 날짜 범위를 제한합니다.

<Canvas of={CalendarStories.MinMaxDate} />

## Range With Buttons

범위 선택 모드와 버튼을 함께 사용하는 예시입니다.

<Canvas of={CalendarStories.RangeWithButtons} />

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>mode</code></td>
      <td><code>{'\'single\' | \'range\''}</code></td>
      <td><code>'single'</code></td>
      <td>선택 모드</td>
    </tr>
    <tr>
      <td><code>value</code></td>
      <td><code>{'Date | null | [Date, Date] | null'}</code></td>
      <td>-</td>
      <td>선택된 날짜 (모드에 따라 타입 변경)</td>
    </tr>
    <tr>
      <td><code>onChange</code></td>
      <td><code>{'(date: Date) => void | (range: [Date, Date] | null) => void'}</code></td>
      <td>-</td>
      <td>날짜 변경 콜백</td>
    </tr>
    <tr>
      <td><code>showButton</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>하단 취소/확인 버튼 표시</td>
    </tr>
    <tr>
      <td><code>showDayHeader</code></td>
      <td><code>boolean</code></td>
      <td><code>true</code></td>
      <td>요일 헤더 표시 (일 월 화 수 목 금 토)</td>
    </tr>
    <tr>
      <td><code>minDate</code></td>
      <td><code>Date</code></td>
      <td>-</td>
      <td>선택 가능 최소 날짜</td>
    </tr>
    <tr>
      <td><code>maxDate</code></td>
      <td><code>Date</code></td>
      <td>-</td>
      <td>선택 가능 최대 날짜</td>
    </tr>
    <tr>
      <td><code>onCancel</code></td>
      <td><code>{'() => void'}</code></td>
      <td>-</td>
      <td>취소 버튼 클릭 콜백</td>
    </tr>
    <tr>
      <td><code>onConfirm</code></td>
      <td><code>{'() => void'}</code></td>
      <td>-</td>
      <td>확인 버튼 클릭 콜백</td>
    </tr>
    <tr>
      <td><code>className</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>추가 CSS 클래스</td>
    </tr>
    <tr>
      <td><code>style</code></td>
      <td><code>CSSProperties</code></td>
      <td>-</td>
      <td>추가 인라인 스타일</td>
    </tr>
  </tbody>
</table>
